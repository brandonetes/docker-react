sudo: required
services:
  - docker

before_install:
  - docker build -t brandonsdockerhub/docker-react -f Dockerfile.dev .



script:
  - docker run -e CI=true brandonsdockerhub/docker-react npm run test
  